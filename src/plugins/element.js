import Vue from "vue";
import { 
    Form,
    Input,
    Button,
    Select,
    Option,
    FormItem,
    Upload
    // Message
} from "element-ui";

// Vue.prototype.$message = Message

// Message.install = function(Vue, options) {
//     Vue.prototype.$message = Message
// }

Vue.use(Form)
Vue.use(Input)
Vue.use(Button)
Vue.use(Select)
Vue.use(Option)
Vue.use(FormItem)
Vue.use(Upload)
// Vue.use(Message)